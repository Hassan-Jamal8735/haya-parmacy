# Protect .env file
<Files .env>
    Order allow,deny
    Deny from all
</Files>

# Protect config and includes directories
RewriteEngine On
RewriteRule ^(config|includes)/ - [F,L]

# Prevent directory browsing
Options -Indexes
